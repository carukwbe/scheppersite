import { ScrollObserverDirective } from './scroll-observer.directive';

describe('ScrollObserverDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollObserverDirective();
    expect(directive).toBeTruthy();
  });
});
